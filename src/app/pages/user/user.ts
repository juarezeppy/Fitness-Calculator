// A user class to be used to define user data from a the DB onto the frontend

export class User {
    ID: string;
    Name: string;
    BMI: number;
    BMR: number;
    TDEE: number;
    ImageURL: string;
    Weight: number;
    Age: number;
}
